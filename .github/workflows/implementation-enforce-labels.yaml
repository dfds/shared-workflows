name: Enforce PR labels

on:
  pull_request:
    types: [labeled, unlabeled, opened, edited, synchronize]
    branches: [ "master", "main" ]
jobs:
  enforce-label:
    runs-on: ubuntu-latest
    steps:
    - uses: ./.github/actions/automation-enforce-release-labels.yml